
/**
 *
 */
class Init {
    /**
     *
     */
    constructor(...args) {
        if (args.length === 0) {
            // 默认 dom, css, event
        } else {
            if (typeof args[0] === 'Object') {

            }
        }
    }
}
